/// Central font scale for the entire app.
/// Change [scale] here to resize all text at once.
class AppFontSizes {
  AppFontSizes._();

  /// 1.0 = normal size, 2.0 = double size.
  static const double scale = 1.5;
}
